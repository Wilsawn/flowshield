// get_credential_status.cdc
// Read-only script to get full credential details.
//
// What to implement:
// - Take an address as input
// - Return all credential fields: tier, riskScore, expiresAt, isExpired, isValid, proofHash
