[build]
  command = "npm install --force && npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://flowshield-production.up.railway.app"
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
