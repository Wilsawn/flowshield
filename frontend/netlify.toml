[build]
  command = "npm install @rollup/rollup-linux-x64-gnu --no-save && npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://flowshield-production.up.railway.app"
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
