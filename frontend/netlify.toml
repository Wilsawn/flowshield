[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://flowshield-production.up.railway.app"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
