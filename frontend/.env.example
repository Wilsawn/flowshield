# Copy to .env.local (same folder). Vite loads .env.local automatically.

# Skip login gate for /dashboard, /copilot, /operator — dev server only (`npm run dev`).
VITE_DEV_AUTH_BYPASS=true

# Optional — backend base URL (defaults to http://localhost:3002 in dev)
# VITE_API_URL=http://localhost:3002
